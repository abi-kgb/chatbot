[providers]
providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "gcc", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && daphne -b 0.0.0.0 -p $PORT whatsapp_clone.asgi:application"
